import 'package:flutter/material.dart';

class Oprojektu extends StatelessWidget {
  const Oprojektu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('O nama');
  }
}
